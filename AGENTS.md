# AGENTS.md

Read `CLAUDE.md`. It is the operating contract for every agent in this repo, whichever model you are: what we're building, the architecture rules, the build order, and the section on working in parallel (your own worktree and branch, the gates, who folds). `docs/ARCHITECTURE.md` is the spec; where it and the older docs differ, it wins.
