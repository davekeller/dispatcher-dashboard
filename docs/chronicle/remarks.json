{
  "2026-09-03": {
    "headline": "Design docs at five, a running board by seven",
    "body": "The brief, layout, flows and architecture spec land first, then a fresh Vite scaffold — earlier patterns ported by hand rather than forked, so reviewers reading package.json find no baggage (decision 14). The whole vertical slice follows in ninety minutes: seeded PRNG and a simulated clock, pure HOS math with boundary tests, fifty drivers with ten planted scenarios, rules and filters as config arrays, the three-pane shell, the Lookout rail, and the route file. Two fixes that night were the interesting ones — limit rules were speaking for drivers who had gone dark, and rank was jostling cards on sub-minute ties. A late pass tore out the left nav for a product bar and stood the route file's stops up on a vertical spine."
  },
  "2026-09-04": {
    "headline": "Lookout gets a face, then a voice",
    "body": "The morning is all co-pilot. The route rail becomes a case file — a vertical, proportional duty timeline with the stops as nodes and the active one following scroll (decision 35, superseding the horizontal ribbon shipped the night before). Lookout gains an illustrated face, a one-line read of the shift, and tabs; by midday those tabs have settled on Chat and Timeline, with recommendations as a sticky bar over the chat and the shift's append-only log beside it. The neutrals get retinted twice — warm to blue-slate, then onto the bento key — on the same argument each time: leave coral and the status bands as the only warmth on the board. Then the header clock stops being a readout and becomes the demo control itself, carrying the scrubber and the explained demo actions. The Lookout bar's recommendation cards are compacted to fit beside the chat, and the reassign picker gets pure, tested candidate ordering and filters."
  }
}
