# DECISIONS

One line per meaningful choice or cut. Newest at the bottom.

| # | Decision | Why |
|---|---|---|
| 1 | Exceptions-first: primary surface is a ranked feed, not a fleet table | A dispatcher intervenes; she doesn't monitor |
| 2 | HOS derived from duty segments against a live clock | Computed, never hardcoded; segments generalize to other rules |
| 3 | Alerts and filters are config arrays | Adding a rule is one object; cheap to extend, easy to read |
| 4 | Staleness is a first-class state with its own band | Unknown + high-stakes is the worst state a dispatcher can be in |
| 5 | Co-pilot reads the same alerts array as the main view | One source of truth; two presentations |
| 6 | Every action confirms before it commits | Consequential actions should feel consequential |
| 7 | Board and map are Phase 2, behind a toggle, never on the critical path | Scope discipline; the list is the product |
| 8 | Simulated shift clock anchored at 2:47 PM, ticking live, with simulated pings for online drivers | Deterministic demo at any hour; scrubbing never blacks out the fleet |
| 9 | Route file is a full page, not a side sheet | Receipts and the ribbon need the room; Lookout stays open beside it |
| 10 | Main view is a region-column board ranked by urgency within columns; the list view is dropped | Top row = the worst problem in each region, which is how a dispatcher thinks about a fix |
| 11 | Offline inside the watch window fires at act-now severity | Making contact can't wait; every minute blind is lead time lost |
| 12 | `Delivery` is a named entity; stops carry arrival, departure, outcome | Deliveries carry attributes, and receipts need them |
| 13 | The co-pilot is Lookout; coral is its only color | Names the job; doesn't pretend to be a person; nothing else wears coral |
| 14 | Fresh scaffold; patterns from earlier work ported by hand, not forked | package.json is read as closely as the code; no drag-and-drop, theme, or SDK baggage |
| 15 | Behind-schedule and won't-finish rules ship in Phase 1 | The ribbon and the rules share one math; five rules make the pattern obvious before the live change |
| 16 | Driver phone and map are Phase 2, but positions and work-order fields are seeded from day one | Cheap now, expensive later |
| 17 | Phase 1 deliberately exceeds the 2–3h guidance; the README states the real time spent | Core scoped with discipline; extras labeled as extras |
| 18 | Scenario anchor moved from 12:47 PM to 2:47 PM | A driver near the 11-hour limit has been on duty ~12h; a 2:40 AM start reads as freight, a 12:40 AM start reads as a bug |
| 19 | Lookout reads derived state directly; pages set a focus driver through context, no per-page portal | Simpler to read on a shared screen; same behavior |
| 20 | Limit rules speak only for drivers who are driving or on duty and visible; the offline rules own dark drivers; a break pauses the alert | One card per driver with one story; found when the won't-finish rule doubled Dre's offline card |
| 21 | Rank sorts time-to-limit in whole minutes | Sub-minute differences between a driver at a dock and one on the road were swapping cards every tick |
| 22 | Generated drivers mostly run within a few minutes of plan; about one in twelve runs late | The first generator crowded the Watch band with a dozen behind-schedule drivers and diluted "exceptions first" |
| 23 | TICK_MS = 5000 with minute-precision countdowns | No false precision; seconds would jitter and mean nothing at a 5-second ping |
| 24 | All 18 text/ground token pairs pass AA (`node scripts/contrast.mjs`) | Watch text is darker than its fill on purpose so it clears AA and stays distinct from Lookout's coral |
| 25 | Planted days are built in one walk so segments and receipts agree | A review of the pure layer found Marcus delivering five stops inside an unbroken driving segment |
| 26 | `scheduleReset` writes the planned legs and service as planned segments | With only an off-duty block, service time was charged as driving and the reset landed 11 min over |
| 27 | Every rule but the offline pair is gated on visibility | A dark driver was collecting three cards' worth of reasons |
| 28 | Late means a projected ETA past the delivery window, not past the plan | With the world frozen, plan drift made 40 of 50 drivers "behind" within an hour of demo time |
| 29 | Drive time left is a quantity; the limit's clock time is a projection. The header shows one, the ribbon the other | They are different questions, and labeling them as such is more honest than forcing one number |
| 30 | The generated fleet is a simulated day; `materialize` applies the clock each tick, and planted scenarios hold still | A frozen world made everyone late within an hour; now scrubbing an hour shows stops completing, and the heroes stay put for the demo |
| 31 | No left nav; the product bar reads Dispatch | The board is the whole product for now; a nav to placeholder pages was ceremony |
| 32 | Board groups by status by default, Act now leftmost | Lena acts on the left; region is one click away for "where are my problems" |
| 33 | Route file: one driver card, alerts, then stops on a vertical spine with the now and limit marks; the horizontal ribbon is gone | Attached to the stops, "where is he and can he make it" reads better than a proportional time axis |
| 34 | Filter bar is dropdowns on one row with search at the right | Chip rows wrapped beside the open rail |
| 35 | Route file gets a sticky left rail in the case-file pattern: a vertical, proportional duty timeline with the stops as nodes, now and limit marks, active node following scroll, click to jump; collapses to the bar and dots. Supersedes 33 | Dave's directive: a vertical combination of the Duty Today line and the stops line, in a case-file navigation pattern |
| 36 | Lookout has a face, a one-line read of the shift, and two tabs, Alerts and Chat; Alerts shows the top three with the rest behind a count | The rail felt like a lot; the top few is what Lena reads |
| 37 | Chat ships as three matched intents in Phase 1, not a model | The tab needed real content; the pattern is the point, and replies are the same cards |
| 38 | Over-limit chips are solid dark red with white text | The one state that must never be missed |
| 39 | Neutrals retinted from warm to blue-slate | The warm ramp read yellow; cool grays leave the coral and the bands as the only warmth |
| 40 | Neutrals settle on the bento key: faint violet-gray canvas, hairline borders, 16px cards | Dave: "more like the bento theme" |
| 41 | Route rail labels stack on two lines (customer, then stop and time); hour marks are ticks, not text | Horizontal room is short in the rail; vertical room is not |
| 42 | Illustrated placeholder avatars, deterministic per driver, gender from the first name | Faces read faster than initials; real photos replace one component |
| 43 | Lookout's tabs are Chat and Timeline, in the claim-rail pattern: recommendations as a sticky bar over the chat, expanded by default; the timeline is the store's append-only shift log; one composer on both tabs | Dave's directive; the route rail already owns the driver's timeline, so this one is the shift's |
| 44 | Route rail narrowed to 11rem | Horizontal room beside the open Lookout is short |
| 45 | Active Shift opens with an ink directive hero; its metrics are divided readings, not cards | Cards now mean routes everywhere on the board |
| 46 | Status columns collapse to animated 40px count rails; On break and Clear start closed | Intervention lanes get the room, while quiet work stays one click away |
| 47 | Board cards lead with route id; driver, truck, and region are assignment metadata | The object being opened and operated on is the route, even when its driver changes |
| 48 | The board control bar starts with Lookout ordering, then one combined route-filter menu, search, and the Status/Region lens | Matches the order → scope → find → view rhythm without importing another product's brand |
| 49 | The route rail leads with an independently collapsible light 2×2 status grid for Progress, Remaining, Schedule, and HOS fit, then an independently collapsible route-order stop spine with an explicit limit crossing. The whole rail still has its narrow collapsed state. Supersedes 35 | A dispatcher can read progress and risk in seconds or reclaim vertical and horizontal room without a heavy inset block competing with the timeline |
| 50 | Board cards use the same stop spine and HOS crossing as the route rail, reduced to a compact unlabeled instrument | The board becomes a true overview of route progress instead of switching to an unrelated horizontal drive-time bar |
| 51 | Route details always render one timeline node per main-content stop; the board uses that same spine as the full left edge of each card's content. Supersedes 49's collapsed completed history | The visual relationship stays literal at detail level and recognizable at overview level |
| 52 | Replace the active-shift directive hero with a compact metrics-only status band ordered Act now, Watch, Offline, On break, Clear, then On shift. Supersedes 45 | The dispatcher sees the board's exact operating state and highest-priority work first, without introductory copy |
| 53 | Detail timeline rows lead with the marker, then time above stop number and customer name; delivered is a green check, viable undelivered is gray, and failed/past-due/post-HOS is red. The board spine uses the same colors without labels | One visual language now answers progress and intervention risk at overview and detail scale |
| 54 | Reserve the board and Lookout card header badge for the highest-priority Over limit, Approaching limit, or Behind schedule state beside the countdown. Render ping age as muted top-right text in the driver row and secondary reasons as plain text | Severity remains prominent while freshness and supporting context stop competing with true priority states |
| 55 | Retune the visual system to warm paper neutrals, near-black type, accessible orange, and a decorative peach-to-rose-to-periwinkle AI spectrum. Reserve that spectrum for Lookout rings, input borders, and the metrics wash; status colors remain semantic. Supersedes 13, 39, and 40 | The product should feel native to modern operations software while retaining its own name, mark, layout, and accessible palette |
| 56 | Keep board-status counts on the left of the light shift instrument and continue the same row on the right with Delivered, To deliver, Total stops, and Delivered percentage. Remove the nested stop-volume header, progress bar, route count, and failed footer | Dispatchers can scan intervention load and throughput as one aligned metric row without returning to a heavy hero treatment |
| 57 | Make `docs/VISUAL_LANGUAGE.md` the visual-judgment guide and link it from the README; keep architecture authoritative for behavior | The warm operations foundation, semantic status rules, AI spectrum boundary, accessibility requirements, and extension checklist should be reviewable without reverse-engineering component classes |
| 58 | Apply the case-file split to stop receipts and board cards: identity plus a small event/route spine on the left and 2×2 scan facts on the right. On board cards, flatten those facts into a full-bleed divider grid rather than an inset panel. Remove redundant board-card headings and reduce miniature route nodes | Chronology reads vertically while comparable metrics scan as a grid, producing one consistent card grammar from board overview to route detail without adding another visual layer |
| 55 | Replace illustrated driver avatars with a deterministic pool of local fictional portraits; give Lookout a watchful asymmetrical eye and insight spark | Real faces make assignments scan faster without a network dependency, while the co-pilot remains visibly its own branded character |
| 59 | Reuse the board card's route header, miniature stop spine, centered driver row, and full-bleed divider grammar for Lookout recommendations; keep evidence and actions in place of the board metrics | The recommendation rail now belongs to the same operational system while preserving the explanatory content that makes an AI recommendation useful |
| 60 | Keep board facts in a 2×2 grid, but organize each vertically centered route stop receipt as three parts: stop number/name/address, a horizontal one-row event track, then one four-column fact row with slightly larger values | Identity leads from the left while chronology and facts each become a compact horizontal scan |
| 61 | Add a right-edge ellipsis to every stop receipt for one-stop reassignment, persistent dispatcher notes, and confirmed cancellation. Cancellation removes unresolved work, resequences the active route, logs the action, and supports Undo | Stop-level maintenance stays adjacent to the stop without crowding every receipt with permanent buttons or creating cosmetic-only actions |
| 62 | Give the live route board its own cool porcelain ground while keeping the app canvas and detail surfaces warm | The lighter, less-yellow field makes a dense set of white route cards feel crisper without flattening semantic status washes or changing the broader warm palette |
| 63 | Turn the miniature card timeline into a subtle focus lens: compress six or more leading completed stops, then slightly enlarge and spread the next two stops. Keep short and completed routes uniform, and keep the detail rail literal | Dense completed history remains visible but recedes, while the work the dispatcher still has to manage receives a modest share of the card's limited vertical space |
| 64 | Grade completed miniature timeline paths and stops from soft sage at the route origin to dark clear-green at the current edge, with shared semantic endpoints available to the future map view | Progress reads as direction and accumulation instead of a flat green line, while timeline and map can later describe the same stop state with the same color language |
| 65 | The dev panel became the simulated-shift control: the clock is the button, the panel anchors under it, says what the simulation is, and explains each demo action | People didn't find a panel at the bottom; anyone opening the app needs to know at a glance that this is a simulated day they can scrub |
| 66 | Recommendation cards in the Lookout bar are compact: avatar beside the directive, countdown on the right, no header row | The title already carries the name; one row less per card is the whole point of the bar |
| 67 | Apply the same gradient and subtle next-stop focus to the route-detail rail, compacting only delivered rows while keeping labels, full click targets, failed stops, and one-to-one receipt mapping | The detail and overview timelines now speak the same visual language without sacrificing the route rail's navigation job |
| 68 | Deepen the five operational hues, give board lanes dedicated stronger washes, and color every Act now → Clear total with its matching status hue; slightly enrich the AI metrics wash | Status sequence and column structure gain contrast while inline alert surfaces remain light and the AI-inspired palette stays subordinate to operational meaning |
| 69 | Make the board card's Stops and Next cells value-first two-line facts, remove completion percentage and “up next,” and tighten the row | The timeline already communicates progress and order, so the grid can spend less height repeating the same information |
| 70 | On homepage and Lookout card timelines, compress only when completed work is at least two-thirds of the route; allocate roughly two-thirds of the spine to that history and the final third to remaining stops, whose markers are only 1% larger. Supersedes 63 for miniature timelines | The route still favors remaining work without the severe truncation or conspicuous magnification of the earlier fisheye |
| 71 | Compact recommendation cards keep the board card's header row (band dot, route id, priority chip, countdown) above the wrapping directive; that header is one shared `RouteHeader` used by the board card and both Lookout card forms | The bar's cards should read as the same object as the board's, and an act-now card must never lose its over-limit chip or its time; sharing the row means the two surfaces cannot drift (supersedes the "no header row" half of row 66) |
| 72 | The reassign picker mirrors the board's nav: an order menu (Lookout orders by default, closest first, most drive time left), manual filters (region, same region only), search on the right; the board's `OrderDropdown` and `FiltersDropdown` take their options as props so both surfaces share one chrome | Dispatchers learn one navigation; orders and filters only re-sequence and narrow the capacity-safe candidate set, so a filter can never add back someone who would enter act now |
| 73 | Board columns run Act now, Watch, On break, Offline, Clear; Act now, Watch, and On break start open, Offline and Clear start as rails; the hero metrics follow the same order | Dave's call (2026-09-04): the Offline band only holds dark-but-clear drivers, so an open, usually empty Offline column was spending width that On break uses; the rail still shows its count |
| 74 | Match each shift-status label, dot, and value to one strong Act now → Clear hue, and widen the four-column throughput block while keeping one metric row | Status reads as a coherent mirrored legend for the board, and delivery volume gains enough spacing to scan independently |
| 75 | Merge the board card's route header and driver row into one assignment/status block: route over driver on the left, priority/countdown over freshness on the right. Supersedes the board-header portion of 71 while Lookout cards retain `RouteHeader` | Related identity and live status now scan as two aligned stacks, removing a divider and an otherwise redundant board row |
| 76 | Set the board card's stop count and label on one line, with the value first (`14 / 16 Stops`) | The most common progress fact becomes a faster left-to-right read and uses less vertical space |
| 77 | Give Lookout an original segmented horizon-ring mark and expand its decorative spectrum to ember, gold, rose, violet, and blue across the avatar, composer, tab, ordering control, and pinned recommendation | A consistent warm-to-cool AI signature now reads immediately across every Lookout entry point without borrowing an external logo or confusing the gradient with operational severity |
| 78 | Deepen the board's cool porcelain ground from near-white to `#f1f3f6`, keeping cards and foreground controls white | The chosen hue remains calm and cool while white working surfaces gain clearer depth and hierarchy |
| 79 | Allocate compressed mini-timeline remainder by route percentage: every unfinished stop keeps one equal full-route slot at the bottom, all dots stay one size, and only completed history tightens. Supersedes 70's fixed final-third allocation and 1% size bump | Remaining work is highlighted without route-to-route scale distortion or a fisheye effect; five stops left on a 20-stop route occupy roughly the final quarter |
| 80 | Restore the Dispatch lockup and active Board control as compact `ink` surfaces with white type and icons | The warm-tinted near-black adds an industrial anchor to the light dashboard without introducing pure black or a heavy full-width header |
| 81 | Keep every mini-timeline stop on one uniform route scale; use 5px nodes for successful completed deliveries and fixed 6px nodes for everything unresolved or exceptional. Supersedes 79 and removes all progress-based compression and magnification | The route length stays visually honest while completed history becomes connective texture and remaining work receives one simple, stable emphasis |
| 82 | Replace Lookout's segmented ring/lens mark with an original full-spectrum disc carrying a white line-sparkle constellation; supersedes the mark geometry in 77 | The gradient becomes the recognizable AI field while the foreground icon stays simple, legible, and clearly distinct from the visual reference |
| 83 | Keep the warm-black product logo icon-only, with the Dispatch wordmark separate on white; retain the matching dark selected Board control. Clarifies 80 | The truck tile supplies the industrial accent without turning the product name itself into a button or lockup block |
| 84 | Raise the chroma of all five homepage status families and their lane washes; change Lookout's mark to a centered radial spectrum with one large and one small line sparkle | The board gains the energetic color contrast of the AI reference while status semantics remain explicit, white cards remain foreground, and Lookout's mark becomes simpler and more optically balanced |
| 85 | Increase board and Lookout recommendation card keylines from 1px to 1.5px without darkening the neutral border color | White foreground surfaces gain slightly more physical definition against the cooler background without adding harsh contrast |
| 86 | Build Lookout's disc from five overlapping radial color fields rather than concentric radial stops | Gold, orange, rose, violet, and blue remain visible across the tiny icon's full background instead of collapsing into what reads as a colored border |
| 87 | Remove the dark filled background from the active Board navigation item while keeping the truck logo tile dark; supersedes that part of 80 and 83 | One near-black brand anchor is enough, and the lighter Board item stops competing with it |
| 88 | The map lives on the route file as a List \| Map toggle beside the stop actions, remembered as `?view=map`; the fleet map at `/map` stays Phase 2 | Dave's ask (2026-09-04): the question the map answers is "where is this driver in their day", which is a route-file question; a shareable URL costs one search param |
| 89 | Leaflet + react-leaflet as a lazy chunk over OpenStreetMap tiles muted to gray by a CSS filter; legs and markers are CSS-classed so they wear the tokens, and the map draws without tiles | The architecture reserved Leaflet, lazy; CARTO's light basemap now wants an API key, and a gray filter over OSM gives the same quiet ground with only the route in color. The board never pays for the chunk, and the one network dependency degrades to a blank canvas rather than a broken view |
| 90 | The truck's position is derived, never stored: walk the legs from the last departure to the next arrival as of the effective last ping, known arrivals for the simulated day, the projection capped at 96% of the leg for planted routes, drift evaluated at the ping | Same discipline as the HOS math: one clock, one source of truth, and a stale fix that cannot creep because the live clock moved |
| 91 | Seeded stops walk from the previous stop, the leg's drive minutes setting the distance (0.25 km/min, ±30%), reflected inside the region box; the same two rng draws per stop as the jitter it replaced | Independently scattered stops zig-zag across the region on a map and read as fake; now the geography agrees with the schedule, and because the draw count is unchanged nothing else in the generated fleet moved |
| 92 | Restore the shared route title row to board cards: a larger band dot and route id on the left, the priority badge and countdown on the right, with the stop spine aligned directly beneath the dot. Supersedes 75 | Route identity and overall status regain hierarchy, while the dot-to-spine alignment makes the card read as one continuous route instrument and matches Lookout recommendations |
| 93 | Reduce shared route-header status dots to the 8px lane-header scale while retaining dot-to-spine alignment; supersedes the larger dot sizing in 88 | Act now and Watch use one consistent indicator scale, keeping route identity prominent without over-weighting the color marker |
| 94 | Source hero and board-lane names from the same title-cased status labels: Act Now, Watch, On Break, Offline, Clear | The mirrored status summary and board use identical dispatcher-facing language instead of drifting by capitalization |
| 95 | Put driver and truck plate on one line, then replace the board card's 2×2 facts grid with one divided row for completed/total stops and the next stop. Supersedes 58, 69, and 76 for board cards | The badge, countdown, and timeline already carry HOS and route risk; removing their duplicate cells makes each card faster to scan and identifies the plate without a second metadata line |
| 96 | Deepen only the shift instrument into a medium-dark five-color AI-spectrum gradient, with light semantic status colors and warm-white throughput text; keep Lookout recommendations on the light wash. Supersedes the light-hero treatment in 55 and 68 | The two gradient surfaces no longer blend together, the hero gains hierarchy without becoming a heavy near-black block, and Act Now through Clear retain their role as a readable color legend |
| 97 | Combine driver/plate, stop count, and next stop into the board card's first content row, while restoring HOS fit and Route risk as the two-column row beneath. Clarifies and partially supersedes 95 | Identity and progress compact into one scan line without removing the operational fit and risk signals that dispatchers still need on the board |
| 98 | Lighten the shift instrument to a saturated pastel five-color spectrum and restore dark operational status hues plus near-black throughput text; supersedes 96 | The hero stays distinct from Lookout's near-white wash without turning into a dark banner, and the metrics regain the stronger dark-on-light reading hierarchy |
| 99 | Place an original, brand-free panoramic city and road-network photograph beneath the shift instrument's translucent pastel spectrum and light scrim | The live urban context gives the technical dashboard a sense of place and movement like a photographic instrument, while the strong overlay keeps metrics primary and avoids importing a licensed or recognizable branded image |
| 100 | Move board-card freshness beside the route id as compact title metadata, keep Lookout's shared header form unchanged, and rebalance the driver/Stops/Up next row with two centered fixed-width fact cells | The assignment row gains even rhythm, while route identity, update age, priority, and countdown remain visible together at the narrowest lane width |
| 101 | Promote board-card Won't finish and Offline states into plain colored title metadata after the route id and before freshness; remove the promoted reason from the footer | Route-level operating state sits with route identity without creating another badge, while the right edge stays reserved for the priority badge and countdown |
| 102 | Fully desaturate the shift instrument's city photograph and replace its pastel AI wash with a dark translucent Act now → Watch → On break → Offline → Clear gradient; use light semantic figures and white throughput text. Supersedes 98's light treatment and clarifies 95 | The image provides neutral context while all hue comes from the board's operational language, producing a deeper photographic instrument without confusing shift status with Lookout branding |
| 103 | Stack the truck license identifier beneath the board-card driver name in micro-label styling, widen the Stops and Up next cells, and right-align both value/label stacks | All three first-row readings now share a compact vertical rhythm, the driver cell loses its loose inline gap, and numeric progress aligns consistently to the card edge |
| 104 | Replace the shift hero's multicolor status gradient with a single warm near-black overlay at 70% opacity over the fully desaturated city image; make all metric text monochrome and reserve status color for the small dots. Supersedes 102 | The hero reads as one black-and-white operational photograph instead of another branded gradient surface, while the minimum semantic color needed for scanning remains intact |
| 105 | Color each left-side hero status reading with the exact light wash used by its board-lane header; keep right-side throughput white. Refines 100's monochrome-foreground rule | The dark photographic hero can carry the board's familiar status palette as accessible text without reintroducing a colored background gradient |
| 106 | The fleet map at `/map` reads the board's ranked list and filters, draws quiet trucks as neutral dots and the ones that need Lena with a name-and-countdown pill, hollow when dark; picking a truck draws only its road ahead, focuses Lookout on it, and offers the route file; markers come from a pure `geo/fleet.ts` | The attention rule from the architecture's map plan, on the same one ranked array as every other surface; the map view holds no logic, and the shared Leaflet plumbing in `views/map/leaflet.tsx` keeps the two maps from drifting |
| 107 | Give the Lookout avatar a freeform, turbulence-mapped mesh field spanning electric cyan, cobalt, violet, hot pink, orange, and acid green beneath its white sparkle glyph | The agent gets a more distinctive and energetic identity at every point of use without forcing its broader color range onto operational surfaces or status semantics |
| 108 | Rebuild the board card's first content row as a four-unit grid: driver identity spans two, Stops one, and Up next one; align the 50% divider with the two-column HOS/risk row and use equal padding in both progress cells | The card now has one stable underlying grid at every lane width, with evenly spaced scan columns instead of a flexible identity area attached to a fixed-width metric block |
| 109 | Rebuild route stop receipts as number / identity / event track / facts columns; center a large bare number in the first, normalize fact padding, and float a vertical actions ellipsis at the right edge without a cell or divider | The expanded route reads as one balanced horizontal case-file grid, while actions remain available without consuming a structural column |
| 110 | Split the route's top-level driver card into a padded identity section and an edge-to-edge, equally divided five-column metrics grid | Driving today, On duty, Break, Stops, and Driving left now share one full-width measurement system instead of floating in a padded row with gaps |
| 111 | Add a 3px Act now red left border to the top-level driver detail card whenever its live HOS countdown is at or below zero | Over-limit status gains a strong full-card edge cue in addition to the badge and negative countdown, without washing the card in alert color |
| 112 | Reduce the shared card radius from 14px to 10px while leaving 8px controls and pill chips unchanged | Every card surface becomes one step more technical and rectangular without collapsing the existing shape hierarchy |
| 113 | Fold all active alert rows and actions into the top-level driver header card, then render a shared rail/receipt marker immediately left of every stop number | Route context becomes one header instrument above the stop list, and delivered, viable, late/HOS, failed, and unassigned states can no longer drift between the rail and their detailed receipts |
| 114 | Picking a driver on the fleet map draws their whole route, completed stops included, through the same `RouteOverlay` the route file's map uses, and frames the route instead of panning to the truck; Fit all clears the pick and re-frames the fleet | Dave's call (2026-09-06): the map should show where the day has been as well as where it is going; one overlay keeps the two maps identical, and framing the route answers the click better than a zoom onto the truck |
| 115 | Pin the route rail flush to the left edge and default it collapsed; give receipt markers and numbers stable subcolumns, reduce the number scale, and match its sans-serif semibold weight to the rail sequence | The stop grid receives more working width by default, while one- and two-digit stops keep a consistent marker gap and typographic relationship to their navigation counterparts |
| 116 | Keep driver identity and five route metrics as one two-layer card, but move alerts into a separate divided card where every rule is one horizontal copy-and-actions row. Supersedes 113's embedded-alert placement | The driver instrument remains cohesive while alerts regain their own visual boundary, and actions stay directly associated without adding a second row |
| 117 | Add a subtle 1px vertical stripe texture to the shared critical chip tone | Over limit remains dark red and accessible but gains a second visual cue that distinguishes the hard-limit badge from softer Act now states everywhere it appears |
| 118 | Run a continuous one-pixel route spine behind the stop-receipt markers and through the gaps between cards, reusing the rail's graded green, neutral, and red states | The stop list now reads as the expanded form of the same timeline rather than a stack of cards that merely repeats its icons |
| 119 | Left-align the board card's Stops and Up next stacks, enlarge their values from 11px to 13px, compact the count to `14/16`, and reduce the first content row by 8px. Supersedes 103's right alignment | Progress facts gain hierarchy and align naturally with the rest of the card while the tighter notation, padding, and row remove unused space without colliding in narrow lanes |
| 120 | Give the selected Board/Map item a warm-neutral fill, inset keyline, and stronger foreground, and reuse that exact state for Chat/Timeline. Supersedes 87's no-fill active Board treatment and the Lookout gradient underline | Both navigation pairs now communicate selection with one visible but quiet pattern instead of two unrelated cues |
| 121 | Move the receipt timeline into a dedicated gutter outside the cards and make each card begin with a compact bare-number column. Refines 118 | The stop stack now reads as cards attached to one route spine, while the card boundary starts with its primary stop identifier rather than splitting marker and number inside one cell |
| 122 | Replace Lookout's filtered mesh-and-sparkle avatar with an unfiltered spectrum disc and a friendly white pixel face. Supersedes 107's warped field and the foreground geometry in 82/84 | Removing displacement and glow keeps the top-right circle and small reusable instances optically crisp, while the pixel face gives the agent a distinct, approachable identity without changing its gradient signature |
| 123 | Keep Board selected in the product navigation while a driver/route detail is open | Route files are drill-downs within the Board workspace; preserving the parent selection maintains location context while the adjacent breadcrumb identifies the specific route |
| 124 | Give the shared Button an explicit icon-only sizing mode and use it for Lookout's circular submit control | Removing inherited horizontal button padding prevents the arrow from being squeezed inside the fixed composer action and keeps its hit area square |
| 125 | Promote the Stops control row to a sticky white instrument bar beneath the product header | Dispatchers keep the delivered count, batch actions, and List/Map mode in reach while scanning a long route |
| 126 | Give route alert rows a narrow semantic-icon column and add a restrained red border around cards containing critical alerts | The separate alert card gains an immediate non-text cue and a clearer boundary without splitting its explanation and actions into different rows |
| 127 | Make the receipt grid's Priority cell the only delivery-priority indicator across completed, active, and upcoming stops | The yellow badge no longer repeats beneath stop identity and the same right-hand scan column answers priority consistently for every assigned stop |
| 128 | Put route details on the board canvas and reuse the board-strength status washes for structural alert, stale, and late-route surfaces | Board and route file now feel like one operational workspace while white cards and softer label chips preserve the foreground hierarchy |
| 129 | Give Lookout-authored language and agent chrome a dedicated IBM Plex Mono face while retaining the product fonts for shared operational evidence and actions | The agent gains a recognizable technical voice without making route data or dispatcher controls feel like code |
| 130 | Remove the white outline from Lookout's spectrum-disc avatar | The clipped gradient already defines the circular silhouette; eliminating the extra ring makes the mark cleaner at compact sizes |
| 131 | Set antialiased font smoothing, grayscale macOS smoothing, and optimized legibility at the document root | All three bundled type families and their fallbacks inherit one explicit rendering policy instead of depending on a body utility or component-level classes |
| 132 | Stack each compact Lookout recommendation as directive line, supporting-evidence line, then divided action row | Separating the instruction from its rationale restores a clear reading rhythm after the mono brand treatment and keeps actions visually anchored beneath both |
| 133 | Keyline board cards by HOS-limit state: route-detail critical red for `over_limit`, a 30% red mix for `limit_act_now`/`limit_watch`, and neutral borders for other alerts | The board now carries the same severity boundary as route details without turning every Act Now or Watch card red |
| 134 | Shorten the board sort label to “Lookout orders” and render the avatar's layered gradients through one circular clip instead of five circle edges | The control is more concise, and the single clipped silhouette removes the remaining halo that could read as a fuzzy avatar border |
| 135 | Render every vertical route timeline in reverse route order without mutating the source route: route-end, reassigned, and remaining stops lead at the top; completed history descends to the route origin. Reverse the route-file receipt list with its rail, while the shift event log remains newest-first | The dispatcher's next concern is visible before completed history, newly reassigned work lands where attention begins, and the compact card, detail rail, and expanded receipts preserve a literal one-to-one visual language |
| 136 | Give the sticky Stops instrument bar a cool blue-slate navigation surface, stronger slate keyline and shadow, and a near-black icon tile | The persistent control bar now reads as section navigation instead of another white stop receipt while its existing white and black controls retain clear hierarchy |
| 137 | Reduce the route receipt's centered stop number from 26px to 20px while preserving its column, semibold weight, and tabular figures | The stop sequence remains an easy anchor without overpowering customer identity or the right-hand operational metrics |
| 138 | Make the compact `12/20 delivered` reading the Stops navbar's primary title and demote `Stops` to its micro-label | The sticky control bar leads with live route progress instead of a generic section name while retaining clear navigation context |
| 139 | Move each unresolved receipt's batch-selection checkbox out of customer identity and into the right-edge action cluster, immediately after the vertical ellipsis | Selection and maintenance controls now share one predictable edge while customer names regain their full identity column |
| 140 | Stop receipts: the selection checkbox sits left of the ellipsis, free text (delivery instructions, the dispatcher note, the notified stamp) moves out of the identity column into a Notes strip along the bottom of the card, and the card's type steps up one size throughout (13/11px identity, 8px labels, 11–12px values) | Dave's call (2026-09-06): pick-then-act reads left to right, notes were crowding the customer name and address, and the 7–10px scale was hard to read at a glance |
| 141 | When a driver is focused (route file, fleet-map pick), Lookout leads with plans for that route from a pure `lookout/plans.ts`: reassign these stops to this driver with this much room, reset after this stop, notify these customers, call this dark truck, each opening its dialog pre-filled; unknown rules fall back to the alert's words, quiet routes get their status; the chat answers "what should I do about X" with the same plans | Dave's ask (2026-09-06): the recommendations were fleet-level everywhere, so on a route they repeated what the alert strip already said instead of saying what to do about this route; deriving plans from the same helpers the dialogs use keeps every number in agreement |
| 142 | Use one pale blue-slate selected state for Board/Map, Status/Region, and Chat/Timeline navigation, with dark slate type and a restrained keyline | The previous warm-brown selection no longer matches the cooler board palette, while reserving dark fills with white type protects the hierarchy of primary actions |
| 143 | Replace unresolved-stop checkboxes with full-receipt selection: click toggles one card, Shift-click adds the inclusive range from the last anchor, and keyboard Enter/Space follows the same behavior; keep the ellipsis outside the selection target | The larger target is faster in a dense dispatch list, the shared slate wash makes the batch visible, and independent actions cannot accidentally alter the batch |
| 144 | Flatten the sticky Stops control into a full-width, lighter slate navigation band with horizontal keylines only | Removing the radius, side border, and shadow separates section navigation from the receipt-card layer while preserving a clear sticky boundary |
| 145 | Keep the Stops bar action-free at rest; after receipt selection, reveal only `Reassign selected (n)` and leave schedule-reset and notification actions in contextual alert/Lookout flows | The bar preserves working width and exposes only the action justified by the current selection instead of repeating route-level controls |
| 146 | The simulated-shift scrubber covers the whole day, 6:00 AM to 6:00 PM, in both directions, with the 2:47 anchor marked and a −1 hour button; `effectiveLastPingAt` is clamped to the clock so scrubbing back never leaves a ping in the future | Dave's call (2026-09-06): the docs define the day as Lena's 06:00–16:00 shift plus the evening the routes run into, and a four-hour forward-only scrubber showed only the last third of it; duty segments were already clipped to the clock, so the morning came for free |
| 147 | Quiet trucks on the fleet map are 10px ink dots at 82% with a white ring, up from 8px muted dots at 55% | Dave saw seven trucks where the bar said fifty: the quiet dots vanished into the gray tiles. Colorless is the rule, invisible is not |
| 148 | Board and Map are one segmented control in the product bar; a link into a route file carries its origin as router state, so the origin's segment stays selected under the route crumb, Map returns to the map with its pick, and the rail's back arrow goes home to the right view | Dave's ask (2026-09-06): two loose tabs left a route file belonging to neither, and the breadcrumb only made sense from the board |
| 149 | The over-limit keyline on the route file's driver card runs along the top edge instead of the left | Dave's call (2026-09-06): the left edge already carries the rail beside it, and a top rule reads as the card's own status |
| 150 | Quiet compact Lookout recommendations and route plans with translucent panel surfaces, 1px semantic keylines, control radii, no shadows, tighter stack gaps, and lighter action dividers; use restrained red for Critical/Act Now, amber for Watch, and neutral for info/quiet plans | The surrounding AI wash already groups the recommendations; removing the second card-depth layer gives evidence and actions priority, while a color-only boundary restores alert salience without adding surface weight |
| 151 | Summarize compact Lookout cards as one driver label plus one short action-oriented bullet per alert, reuse that compact card in chat replies, and reduce its action-button height, padding, and labels to concise verbs | Multi-rule drivers no longer expand into repeated title/body paragraphs; each line contributes a distinct decision cue while Reassign, Reset, and Notify fit on one footer row |
| 152 | Replace Lookout's Timeline tab with an Artifacts workspace that intentionally shows only an empty state; retain the append-only event data for later use | Dave wants to develop Artifacts separately, so the navigation names the intended destination without prematurely defining its content model |
| 153 | Give the route file a fixed vertical slate field that starts lighter near the header and deepens toward the bottom while receipts scroll over it | A stationary depth gradient visually connects the long stop list and suggests continuation below without adding another container, shadow, or tint to the white operational cards |
| 154 | Make the product bar shadowless and render Board/Map as one edge-to-edge segmented control whose selected fill occupies the full segment; remove the shared selected-item ring and shadow from Lookout navigation too | The active view should read as one half of a switch, not as a small card nested inside another control, and flattening both navigation families keeps their shared selected-state grammar honest |
| 155 | A "Play against the real clock" switch in the simulated-shift panel: a fixed offset (loadedAt − anchor) makes simulated time equal wall time; any scrub returns to the pinned 2:47 shift | Dave's hedge (2026-09-07): the brief requires figures computed against a live clock, which the pinned ticking clock already is, but a panel that wants to see the day on today's clock can |
| 156 | Stop receipt status chips (next, past the limit, past window, needs a driver) sit at the top of the identity column, above the customer name | Dave's call (2026-09-07): the badge is what the eye is looking for when scanning the list; the name follows |
| 157 | Give Lookout Recommends one continuous muted field derived from the avatar's electric cyan, pink, orange, mint, cobalt, and violet, while lifting compact cards from 70% translucent to solid white | The background now belongs unmistakably to the new Lookout identity, but the low color mix and opaque foreground keep recommendations visually primary and operational severity borders intact |
| 158 | On a plain board-card click, use the native View Transitions API to compress the source slightly and expand it into the route driver summary over 380ms, with the remaining route workspace entering just behind it; keep normal navigation for modified clicks and reduced motion, plus a CSS-only workspace entrance where the API is unavailable | The motion preserves object continuity between the dispatch board and route file without animating stable chrome or adding an animation dependency; every fallback remains a real link |
| 159 | Deepen the fixed route-file gradient by reducing the white lift at the top and carrying the offline slate family farther through the middle and bottom | The background supplies more visible page flow and contrast while staying neutral enough for white receipts and semantic alert color to lead |
| 160 | Give every route driver-summary card a 3px semantic top rule; use status red, amber, blue, or medium slate where appropriate and a darker navigation slate for Clear | The route file now begins with a consistent status-bearing edge, while quiet routes retain structure without falsely reading as green success alerts |
| 161 | Move Progress, Remaining, Schedule, and HOS fit out of the expandable route rail and into one horizontal grid in the sticky Stops band; let the rail begin directly with its stop timeline | The operational readings stay visible while dispatchers scroll or switch between List and Map, and the rail is reserved for navigation that directly maps to stop receipts |
| 162 | Give the complete Lookout sidebar one soft, cool-slate shadow cast toward the main workspace in both expanded and collapsed states | Lookout reads as a distinct assistant plane without turning into a heavy floating card or adding shadows to its internal navigation |
| 163 | Give selectable stop receipts a lighter slate hover/selection wash and one layout-stable 2px interaction keyline; preserve blue Next and red risk tones on that keyline | Selection becomes crisp without the previous heavy fill, and hovering cannot shift the receipt grid or hide higher-priority operating status |
